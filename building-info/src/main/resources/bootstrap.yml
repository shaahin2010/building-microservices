spring:
  application:
    name: info-service

server:
  port: 7070
  
eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8765/eureka/
  instance:
    hostname: localhost